name: persistence
description: Drift/SQLite stores for LLMerta — settings, personas, model cache, saves.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  drift: ^2.28.0

dev_dependencies:
  build_runner: ^2.5.0
  coverage: ^1.11.0
  drift_dev: ^2.28.0
  lints: ^6.0.0
  test: ^1.26.0
