name: llm
description: LLM provider clients, model discovery, and agent sessions for Mafia LLMTTG.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  game_core:
    path: ../game_core
  http: ^1.6.0
  path: ^1.9.0

dev_dependencies:
  coverage: ^1.11.0
  lints: ^6.0.0
  test: ^1.26.0
