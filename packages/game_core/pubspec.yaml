name: game_core
description: Rules engine for Mafia LLMTTG. Pure Dart, no IO, no Flutter.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dev_dependencies:
  coverage: ^1.11.0
  lints: ^6.0.0
  test: ^1.26.0
