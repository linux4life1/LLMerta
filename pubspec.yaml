name: mafia_llmttg_root
description: Root package config so analysis_options.yaml can resolve lint includes.
version: 0.0.0
publish_to: none

environment:
  sdk: ^3.12.0

dev_dependencies:
  lints: ^6.0.0
